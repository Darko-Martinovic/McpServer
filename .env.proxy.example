# McpServer Proxy Configuration
# Copy this to .env in your proxy directory

# ✅ CRITICAL: Updated to correct MCP Server URL (port 5000, not 9090)
VITE_MCP_SERVER_URL=http://localhost:5000

# Proxy server port
PORT=5002

# ✅ UPDATED: Azure Search configuration for new mcp-tools index
AZURE_SEARCH_ENDPOINT=https://your-search-service.search.windows.net
AZURE_SEARCH_APIKEY=your-api-key-here
AZURE_SEARCH_INDEX=mcp-tools

# Optional: Enable detailed logging
DEBUG=true